# Claude Code vs Codex：面试对照

四篇问题对齐，协议不是一套。对照时只说「那边有、这边没有」，不要把名字硬翻译过去。

系列索引：[Claude](./claude) · [Codex](./codex)

## 一眼差在哪

| 点 | Claude Code | Codex |
| --- | --- | --- |
| API | Messages：`system[] + messages + tools` | Responses：`instructions + input + tools` |
| 系统提示 | 带 `cache_control` 的文本块数组 | 一整段 `instructions` 字符串 |
| 项目说明 | `CLAUDE.md` 在第一条 user 的 `<system-reminder>`，不在 system | `AGENTS.md` 是 user，但不是手打的话 |
| 工具参数 | `tool_use.input` 线上就是对象 | `function_call.arguments` 线上是字符串 |
| 子 Agent | 一个工具 `Agent`（旧名 `Task`） | `spawn_agent` + `wait_agent` + `agent_message` |
| 默认同步 | 是。同一张 `tool_result` 就是正文 | 否。spawn / wait 回执都不是正文 |
| 异步正文 | 稍后一条 user 里的 `<task-notification>` | 稍后的 `type=agent_message` |
| 有没有 wait | 没有 | 有，三种字符串，不含正文 |
| 沙箱 | `sandbox-runtime` 路径/网络表 | Windows Token + Capability SID + ACL |
| 压缩形态 | `messages` 换成带固定前缀的摘要 user | 本地摘要 user / 远端 v1 opaque / 远端 v2 `compaction_trigger` |
| 长期记忆 | `~/.claude/projects/<slug>/memory/` | `~/.codex/memories/` |
| 检索 | 默认 `Grep`（ripgrep），不是向量，也不是 BM25 | 默认 shell 搜 `MEMORY.md`，不是向量 |

## 01 请求结构

| | [Claude](./claude/01-Claude-Code发给大模型的完整请求结构.md) | [Codex](./codex/01-Codex发给大模型的完整请求结构.md) |
| --- | --- | --- |
| 面试先说 | Messages 三块；`system` 不是一根字符串；工具用 `tool_use` / `tool_result` 同 `id` 配对 | `/v1/responses` 三块；`input` 是有序历史，不是 Chat Completions 的 `messages[]` |
| 不要说 | 和 Chat Completions 一样；`CLAUDE.md` 写进了 system 所以和官方提示同一优先级 | 这就是 `messages[]`；日历一类延迟工具的 Schema 在顶层 `tools` 里 |

## 02 主子 Agent

| | [Claude](./claude/02-Claude-Code主子agent机制.md) | [Codex](./codex/02-Codex主子agent机制.md) |
| --- | --- | --- |
| 面试先说 | 默认父 turn 卡住，`tool_result` 就是子代理最后一段话；异步回执只有 `async_launched` | spawn 回执只有名字；wait 三种字符串不含正文；真正正文在 `agent_message` |
| 不要说 | 也有 `spawn_agent`，回执只有 `/root/security` 这个名字 | `wait` 把子 Agent 的答案当返回值拿回来 |

## 03 工具、审批、沙箱

| | [Claude](./claude/03-Agent工具系统-FunctionCalling-MCP-权限沙箱.md) | [Codex](./codex/03-Agent工具系统-FunctionCalling-MCP-权限沙箱.md) |
| --- | --- | --- |
| 面试先说 | 暴露、审批（allow/ask/deny）、`sandbox-runtime` 是三层；规则在 settings，不在 `CLAUDE.md` | 暴露、审批、Token+ACL 是三层；路径写在 ACL 上，不在 Token 里 |
| 不要说 | 沙箱就是提示词；`CLAUDE.md` 能改安全策略；产品会检测死循环并自动打断 | 用户批准了就等于管理员 Token；产品会检测死循环并自动打断 |

两边共同点，面试可以一起说：没有靠关键词扫描的注入熔断；死循环靠取消、超时、截断、不自动重试；减损不是根治。

## 04 压缩与长期记忆

| | [Claude](./claude/04-Claude-Code记忆系统-压缩与检索.md) | [Codex](./codex/04-Codex记忆系统-压缩与检索.md) |
| --- | --- | --- |
| 面试先说 | 压缩替换当前 `messages`；长期记忆是项目目录下的 `MEMORY.md` + 主题文件；新会话只自动注入截断后的索引 | 压缩替换当前 `input`（本地 / v1 / v2 三种形态）；长期记忆是根会话后台写 `~/.codex/memories/` |
| 不要说 | 压缩完写入 `MEMORY.md`，下次语义检索；这边也有 `compaction_trigger` 和远端 v2 密文 | 压缩完写入 `MEMORY.md`，下次向量检索 |

两条管线，别串成一条。压缩保的是这一轮还能继续说话；长期记忆给的是下一个会话。

## 开口前再过一遍

1. 先报协议名字，再讲三块字段。不要两边都说成 `messages[]`。
2. 子 Agent：先说回执是不是正文。Claude 默认同步是正文；Codex 的 wait 不是。
3. 沙箱：先说「提示词不管执行」。Claude 讲路径表，Codex 讲 Token 和 ACL。
4. 记忆：先拆压缩 / 长期记忆，再否定「向量库」。
